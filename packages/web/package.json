{
  "name": "web",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "tsc:build": "tsc -b",
    "tsc:watch": "tsc -b --watch",
    "dev": "",
    "build": "cross-env NODE_ENV=production && npm run tsc:build && webpack"
  },
  "dependencies": {
    "react": "^16.8.3"
  },
  "devDependencies": {
    "@types/react": "^16.8.5"
  }
}
